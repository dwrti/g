name: Video Direct Uploader (No Limits)

on:
  workflow_dispatch:
    inputs:
      video_url:
        description: 'رابط الفيديو'
        required: true
      file_name:
        description: 'اسم الملف'
        required: true

jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - name: Transfer Video
        run: |
          # 1. تجهيز الاسم
          FINAL_NAME=$(echo "${{ github.event.inputs.file_name }}" | sed 's/\.mp4//g' | tr ' ' '_').mp4

          # 2. التحميل (بأمر صريح ومختصر)
          echo "⬇️ جاري تحميل المقطع..."
          curl -L "${{ github.event.inputs.video_url }}" -o "$FINAL_NAME"

          # 3. الرفع إلى Bash.Upload (يدعم حتى 2GB ورابطه مباشر)
          echo "📤 جاري الرفع..."
          # هذا الموقع يعطيك الرابط في سطر واحد بدون تعقيد
          DIRECT_URL=$(curl https://bashupload.com/ -T "$FINAL_NAME")

          # 4. النتيجة
          echo "--------------------------------------------------"
          echo "✅ تم الرفع بنجاح!"
          echo "🔗 الرابط المباشر:"
          echo "$DIRECT_URL"
          echo "--------------------------------------------------"
